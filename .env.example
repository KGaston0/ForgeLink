# Django settings
SECRET_KEY=your-secret-key-here
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

# Database settings
DB_NAME=forgelink_db
DB_USER=forgelink_user
DB_PASSWORD=your-password-here
DB_HOST=localhost
DB_PORT=5432

# CORS settings (Development only - restrict in production!)
# WARNING: CORS_ALLOW_ALL_ORIGINS=True allows any origin to access your API
# For production, set CORS_ALLOW_ALL_ORIGINS=False and specify allowed origins
CORS_ALLOW_ALL_ORIGINS=True
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:8080
